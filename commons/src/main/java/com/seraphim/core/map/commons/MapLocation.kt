package com.seraphim.core.map.commons

/**
 * User location display on the map.
 */
interface MapLocation {
    /** Show/hide the user's location dot on the map. */
    fun enableUserLocation(enabled: Boolean)
}
